{
  "extends": ["./tsconfig-base.typecheck-node.json", "./tsconfig-base.typecheck-modernonly.json"],

  // This root config only checks files that are outside other projects: every package, demo app, and external-test
  // has its own tsconfig targeting its own files.
  "include": ["scripts/", "*.*"]
}
